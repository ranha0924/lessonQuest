# Phase 1 M5/M6 Final-Gate Remediation Plan 7

Attempt 7 confirms that an open-ended text blacklist cannot prove the Phase 1 learner-output invariant. Replace that boundary with constrained server-owned rendering: export one pure local hint-template function; the deterministic provider uses it; the repository independently computes the expected content from the already-authorized concept/simulation summary and requested level; `validateHintOutput` accepts only an exact content match in addition to schema, target, level, alphabet, URL/markup, and answer checks. Add RED cases for the newly reproduced direct answers/URLs/markup and a mismatched otherwise-safe sentence. Existing safe local outputs must remain GREEN. Run all verification and require an eighth fresh independent score above 85 without blocker. This is local-only and reversible.
