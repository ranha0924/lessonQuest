# Phase 1 M5/M6 Final-Gate Remediation Plan 6

Attempt 6 demonstrates that blacklist-style script handling is insufficient. Phase 1 uses only the deterministic Korean/English local provider, so make its output alphabet explicit: printable ASCII plus Korean Hangul/Jamo and ordinary ASCII whitespace. Reject every other code point, including Greek/Cyrillic homoglyphs, format controls, combining marks, emoji, and enclosed-letter symbols. For structural checks, remove whitespace/format/marks while preserving URL/HTML punctuation, so invisible characters cannot split schemes or tags. Add the reproduced homoglyph and structural variants as RED tests, retain all prior and safe-hint controls, run full verification, and require a seventh fresh independent score above 85 without blocker. No external service or data is added.
