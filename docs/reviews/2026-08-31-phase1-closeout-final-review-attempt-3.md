# Phase 1 Closeout — Independent Final Review, Attempt 3

Date: 2026-08-31. Reviewer: `/root/phase1_closeout_final_validation_3`, newly assigned and not an implementer or either previous final reviewer.

**Verdict: 96/100 PASS, no critical or blocking finding.** The frozen local candidate satisfies the mandatory independent final-validation gate, which requires strictly more than 85 points. This verdict does not certify a main merge, remote CI, deployment, or live Vercel interaction. Those delivery checks remain required before reporting deployment complete.

Preserve the original **82/100 FAIL** in `2026-08-31-phase1-closeout-final-review.md` and **85/100 FAIL** in `2026-08-31-phase1-closeout-final-review-attempt-2.md`. Both files remained byte-identical throughout this review.

## Independence, candidate and scope

- Worktree: `.worktrees/phase1-closeout`; branch: `codex/phase1-closeout`.
- Base: `fe206c1f9f69e10dbc15a7abc656d0b1636ad93d`; planning HEAD: `4913f808306fee68d2b63411bd37dde020c217ea` plus the tracked and untracked implementation candidate. Reviewing only HEAD would omit the implementation.
- Frozen runtime/test/tooling inventory: **42 files**, manifest digest `67528a28b671f9790124241acb7b4712b649c16e1950df10181042b61a23b0be`, recorded in `/tmp/lessonquest-closeout-candidate-3-manifest.json`. I independently verified every file hash before and after execution, and compared the inventory with the complete changed/untracked runtime file set: **zero omissions and zero mismatches**.
- Read `AGENTS.md`, `docs/PROJECT_CANON.md`, the project-memory final rubric, the 97-point closeout plan and its amendments, the 96-point development-preview expansion, the 99-point uncertain-replay remediation, the 97-point pending-resume remediation, their plan reviews, both failed final reviews, implementation verification and completion documentation.
- Inspected the complete base diff and every new implementation/test/configuration file, including dependencies, CI, build flags, asset and crypto handling, runtime ownership, transport, UI recovery, repository locks and failure classification. The latest RED log independently confirms both lifecycle cases originally returned 200 instead of the required 409; the focused 61-test GREEN evidence was also inspected.
- No implementation edit, commit, merge, push, external-system change or deployment was made by this reviewer. Three temporary synthetic probe files were copied into the test directory only after the whole check finished, then removed before browser/build and final containment checks. Their sources and logs remain under `/tmp`. This report is my only durable repository addition.

The approved product boundary is the synthetic Phase 1 vertical slice, with actual React components → HTTP client → Hono → repositories → fresh PGlite and deterministic Rasa. The authorized Vercel preview runs those components and services in one browser tab. Its browser-owner-controlled authentication and database are explicitly unsuitable for real records. This review does not silently expand acceptance to production authentication, external AI, persistent server storage, real students, Firebase, multiple devices or a multi-connection PostgreSQL deployment.

## Findings and disposition

### Previous blocker 1 — Resolved: nonterminal replay preserves the uncertain original

`apps/web/src/components/student-play.tsx:252` now distinguishes the finite terminal Rasa codes from an error that only describes a replay. An actual 409 while the original request is RUNNING, a temporary authorization 404 and an audit-storage 500 do not release the earlier uncertainty or replace its UUID. The four original independent probes pass unchanged. Successful exact replay restores the authoritative sequence and produces one request/action/usage/hint effect; subsequent answer and completion succeed.

The prior independent terminal probes also pass: REJECTED, TIMED_OUT and finalization FAILED resolve the uncertain outcome correctly. Exact replay does not rerun the provider or mutate terminal evidence, and a new request can obtain the same-level hint once. The checked-in full suite additionally covers provider FAILED, authorization-revoked FAILED, late provider return after timeout, and remount after terminal outcomes.

### Previous blocker 2 — Resolved: resume waits for stable hint state

`packages/db/src/learning-repository.ts:917` locks the authenticated existing attempt before the organization/attempt-scoped QUEUED/RUNNING query at line 926 and the resume-state reads. Existing user, organization, class, membership, assignment and time checks remain before this pending-state lookup. The lock uses the same attempt boundary as Rasa preparation/finalization and learning writes. `StudentPlay` explains the temporary conflict and leaves the assignment retry available without mounting stale answer/completion controls.

I adapted the prior direct-remount and actual `DevelopmentPreview` role-switch reproductions to the approved server-409 behavior. Both now pass this complete sequence:

1. Disconnect the hint response after the real provider begins; the real request remains RUNNING.
2. Remount the student or switch teacher → student, then request resume before releasing the provider.
3. Receive a real 409 and the waiting message, with no mounted answer or completion controls.
4. Release and await the original provider; request resume again and restore the one committed hint.
5. Answer correctly and complete successfully with event sequences **0–5**, exactly one request/action/usage/HINT_USED/RASA_OPENED, and no stale completion retry.

The resumed sequence is `EXPERIENCE_STARTED:0, QUESTION_ANSWERED:1, RASA_OPENED:2, HINT_USED:3, ANSWER_RETRIED:4, EXPERIENCE_COMPLETED:5`. This replaces the previous `[409,409]` completion failure with successful completion; it is not an assertion that the old mounted-player behavior was preserved.

### Additional boundary probes — Passed

- **Authority before pending state:** disabling the synthetic organization, class or assignment makes resume return 404 while the hint is pending. Teacher-role and wrong-organization requests also return 404. Existing durable membership-revocation checks pass in the full suite.
- **Attempt isolation:** another assignment in the same class can start and resume while the first assignment has a pending hint. The first remains at 409 until its original provider finishes, then resumes at sequence 4 with one hint.
- **QUEUED and multiple request evidence:** a separately inserted synthetic QUEUED request blocks resume even when another request for that attempt already succeeded. Transitioning it to RUNNING still blocks; a valid FAILED terminal transition allows resume with the original single hint. This is a database-state containment probe, not a claim that the current local provider normally queues work.
- **Stranded RUNNING:** a synthetic trigger rejects the terminal-status write after the provider fails. The API safely returns 500, the request remains RUNNING, and resume remains 409 after the storage trigger is removed. No player controls, hint action, usage or hint learning events are created. This confirms the documented fail-closed limitation; it does not claim worker-crash reconciliation exists. The development preview can reset its synthetic in-memory data.

### Nonblocking follow-up

The additional QUEUED, unrelated-attempt and terminal-status-storage probes are currently retained only as independent `/tmp` evidence. Consider making these cases durable beside `apps/web/test/phase1-recovery.test.tsx:35` in a separately planned and reviewed follow-up. The newly fixed direct-remount and actual-role-switch cases are already checked in. No implementation change is requested during this frozen scoring pass.

## Fresh independent execution

| Check                                           | Independent result and evidence                                                                                                                                                                                                                                                                                                                                                                                                 |
| ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `corepack pnpm check`                           | Exit 0; **31 files / 339 tests**, ESLint, Prettier, root/web TypeScript checks, all workspace builds and ordinary web build passed. Vitest duration **50.17 s**. `/tmp/lessonquest-independent-attempt3-check-final.log`.                                                                                                                                                                                                       |
| Adversarial real-boundary probes                | **12/12 passed**, 3 files, **7.95 s**: original replay probes 4, prior terminal replay probes 3, adapted lifecycle probes 2, authority/attempt scoping 1, QUEUED containment 1 and stranded-RUNNING containment 1. `/tmp/lessonquest-independent-attempt3-probes.log`.                                                                                                                                                          |
| `corepack pnpm test:preview`                    | Exit 0; **10 Chromium cases**, desktop/mobile, **20.3 s**. Actual teacher save/validate/sandbox/approve/assign, student wrong/hint/retry/complete, teacher results, first-correct damage 7 versus retry damage 8, role state, reset/reload, reset during write, asset-failure retry and normal-build exclusion passed. `/tmp/lessonquest-independent-attempt3-preview.log`.                                                     |
| `corepack pnpm test:browser`                    | Exit 0; **12 Chromium cases**, desktop/tablet/mobile, **22.1 s**. All measured states, target dimensions, critical contrast, dark surfaces, keyboard focus, fragment navigation and reduced motion passed; deliberate target-size/contrast faults were detected. `/tmp/lessonquest-independent-attempt3-demo-browser.log`.                                                                                                      |
| Actual local response headers and binary assets | Every served security header exactly matched `vercel.json`. CSP permits same-origin connection and WebAssembly without generic `unsafe-eval`; `frame-src 'none'` remains. All 3 same-origin assets matched emitted files byte-for-byte, with correct MIME and WASM magic. **16,778,719 bytes** total: initdb WASM 395,242; main WASM 10,088,161; data 6,295,316. `/tmp/lessonquest-independent-attempt3-preview-headers.json`.  |
| Production dependency audit                     | `corepack pnpm audit --prod --audit-level high`, exit 0: **No known vulnerabilities found**. `/tmp/lessonquest-independent-attempt3-audit.log`.                                                                                                                                                                                                                                                                                 |
| Containment and whitespace                      | **42/42 hashes unchanged**, complete runtime inventory, both failed reports unchanged, temporary workspace probes removed, and full-base `git diff --check fe206c1` clean. Heuristic credential-pattern scan of all 42 files found no matches; it is not an exhaustive secret-detection guarantee. `/tmp/lessonquest-independent-attempt3-final-containment.json` and `/tmp/lessonquest-independent-attempt3-secret-scan.json`. |

The initial independent `pnpm check` stopped at Prettier while the root appended the latest evidence table to `2026-08-31-phase1-closeout-verification.md`. Root formatted that documentation and froze it; no runtime/test/configuration file changed. I preserved that failed command in `/tmp/lessonquest-independent-attempt3-check.log`, then reran the whole command successfully. It is a real initial verification failure caused by concurrent documentation timing, not a product RED or a passing run.

Probe sources are `/tmp/lessonquest-independent-running-hint-probe.test.tsx`, `/tmp/lessonquest-independent-attempt2-terminal-probe.test.tsx`, and `/tmp/lessonquest-independent-attempt3-resume-probe.test.tsx`. The root's latest standalone integration **39/39** and scripted E2E **63/63** logs were inspected; those suites' files are included in my independent 339-test full check, but I did not count their standalone commands as my own executions. React boundary checks use jsdom with actual HTTP/Hono/PGlite; the 22 browser cases use Chromium.

The managed browsers/servers finished. I stopped the separate header-only server after capture and confirmed no listener remained on ports 4178 or 4179. All reviewer execution sessions are released.

## Other reviewed controls and limits

The complete diff preserves teacher validation/approval, immutable published artifacts, server-owned answer scoring and boss projection, aggregate-only student boss responses, exact pending answer/completion event identity and boss-end identity, fixed permitted Rasa output, current-authority rechecks, and trace-correlated redacted diagnostics. Generic finalization storage errors are FAILED with `RASA_FINALIZATION_FAILED`; actual revocation remains DENIED/404. No false terminal DENIED/CONFLICT audit is introduced for the generic storage fault.

The preview transport has no external passthrough and uses separate fixed teacher/student client identities. Reset waits for pending transport work and the serial projection queue before closing the memory database. The narrow SHA-256 adapter matches Node vectors and artifact hashes, rejects unsupported operations, and is aliased only for the explicit preview build. UTF-8 parser limits work without Buffer. The normal build excludes the preview runtime, auth and PGlite asset graph. Both browser suites reject unexpected external traffic and application errors; the preview suite also verifies empty browser persistence and no service-worker registrations.

No Firebase, real student records, reference-repository changes, remote database, external AI, new paid service or real credentials were used. The large first-load payload, reset-on-refresh storage, browser-owner control, Chromium-only browser coverage, lack of durable worker-crash recovery and absence of real PostgreSQL multi-connection/multiple-device proof remain explicit limits, not production-readiness claims.

## Final rubric

| Category                                   |      Score | Evidence and deductions                                                                                                                                                                                                                                                                                                                                  |
| ------------------------------------------ | ---------: | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Requirement and approved-plan conformance  |  **25/25** | Full synthetic Phase 1 flow, authorized actual-UI preview, both independently reproduced corrections and prescribed failure containment are present. Scope exclusions and required later delivery checks remain explicit.                                                                                                                                |
| Correctness and code quality               |  **20/20** | Complete diff review, 339 passing tests and 12 independent adversarial probes support the current retry identities, terminal-state handling, authenticated resume lock and eventual completion. No remaining blocking correctness finding was reproduced.                                                                                                |
| Security, privacy and tenant isolation     |  **20/20** | Authorization precedes pending lookup; organization/attempt scoping, fixed hints, immutable evidence, normal-build exclusion and narrow preview origin/CSP controls work within the explicitly synthetic boundary. No critical exposure found.                                                                                                           |
| Test and verification evidence             |  **18/20** | Full checks, audit, both Chromium suites and independent lifecycle/terminal/authority/storage probes pass. Deduct for the three extra edge probes remaining ephemeral and the intentionally single-engine/local-PGlite verification boundary.                                                                                                            |
| Operability, recoverability and provenance |  **13/15** | Pinned dependencies, original synthetic fixture attribution, visible startup/reset limits, controlled runtime close, rollback guidance and CI gates are adequate for the development preview. Deduct for the large startup payload and recovery of stranded RUNNING state requiring whole synthetic reset; operating recovery is deferred and disclosed. |
| **Total**                                  | **96/100** | **PASS; strictly greater than 85, with no critical or blocking finding.**                                                                                                                                                                                                                                                                                |

This gate applies to the exact 42-file frozen candidate above. A later production/test/configuration change requires evaluation under the project gates. Documentation may record this verdict and subsequent verified delivery evidence without implying that unperformed remote checks passed. Proceed with the already authorized guarded delivery only after this report is finalized; verify exact-head CI, merged result, deployed SHA/status and a live synthetic interaction before claiming deployment complete.
