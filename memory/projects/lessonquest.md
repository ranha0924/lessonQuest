# LessonQuest Project Memory

Updated: 2026-09-01

## Product and source of truth

LessonQuest is a playable learning platform with four product areas: Studio, Play, Rasa, and Institution Console. The canonical product constraints live in `docs/PROJECT_CANON.md`; architecture and sequencing live in `docs/INTEGRATION_PLAN_V2.md` and the applicable plan under `docs/superpowers/plans/`.

The existing GitHub repositories and their deployments are reference sources only. They must remain unchanged. Reuse is limited to selectively copying useful code, behavior, or tests into LessonQuest and recording provenance.

## Standing delivery preference — 2026-08-31

The user explicitly requested that future LessonQuest changes include merging into `main` and deployment. After the existing plan and independent final-review gates pass, complete commit, merge, push to `origin/main`, and deployment to the configured LessonQuest Vercel project without another routine integration-choice question. This supersedes the earlier preference that the user would deploy. Verify required CI, the deployed commit, deployment status, and the live URL; prefer observing the existing Git-triggered deployment over creating a duplicate. This permission does not extend to reference repositories, real student data, destructive operations, or new paid resources. Missing credentials or an unidentified deployment target must be reported, not bypassed. This entry records a prospective workflow instruction, not evidence that any pending PR has been remotely merged or deployed.

## Mandatory pre-implementation validation gate

Every production-code, database-migration, or deployment task must pass a written review before implementation starts or resumes.

### Review process

1. State the implementation scope and the controlling requirements.
2. Write or update a concrete implementation plan with interfaces, affected files, ordered tests, risks, and rollback or containment steps.
3. Score the plan using the rubric below and cite specific plan sections or evidence for every category.
4. Save the review as `docs/reviews/YYYY-MM-DD-<scope>-plan-review.md`.
5. Implement only if the final score is **86/100 or higher** and there are no critical blockers.
6. If the score is 85 or lower, revise the plan and perform a new review. Preserve both the original and revised scores in the review record.

### 100-point rubric

| Category                               |  Points | Passing evidence                                                                                                   |
| -------------------------------------- | ------: | ------------------------------------------------------------------------------------------------------------------ |
| Requirements and scope coverage        |      25 | Acceptance criteria trace to canonical requirements; exclusions and dependencies are explicit.                     |
| Architecture and interface quality     |      20 | Boundaries, data ownership, contracts, failure behavior, and migration/compatibility effects are defined.          |
| Security, privacy, and tenant safety   |      20 | Threats and abuse paths are addressed; authorization fails closed; no real student data or unsafe secret handling. |
| Test and verification quality          |      20 | RED/GREEN tests, integration checks, negative cases, regression scope, and completion commands are concrete.       |
| Execution readiness and recoverability |      15 | File-level steps, dependency versions, observability, rollback/containment, and source provenance are actionable.  |
| **Total**                              | **100** | **Implementation requires more than 85 points.**                                                                   |

### Critical blockers

The gate fails regardless of score if any of these remain:

- an unresolved authorization, tenant-isolation, secrets, or student-data risk;
- destructive or irreversible migration without backup and rollback;
- generated learning content can be published without independent validation and teacher approval;
- untrusted generated code can access credentials or institution/student data;
- a plan mutates an existing source repository or deployment;
- acceptance criteria cannot be verified;
- required external authority, credentials, or user choice is missing.

## Implementation discipline after the gate

- Use test-driven development: observe the intended failing test before production implementation.
- Keep client inputs strict and server authority explicit.
- Prefer synthetic data and local deterministic services.
- Run targeted tests while iterating, then the complete affected verification suite.
- Do not claim completion from the pre-implementation score alone; the implemented result must separately pass its tests, type checks, lint, build, security audit, and any plan-specific checks.

## Mandatory independent final-validation gate

After implementation and the implementing agent's checks, a separate agent that did not implement the reviewed change must validate the actual result.

### Independence and evidence

- The final reviewer must be a newly assigned independent agent and must not edit the implementation during the scoring pass.
- The reviewer inspects the controlling requirements, approved plan, complete diff, tests, dependency changes, and recorded verification output.
- The reviewer runs relevant checks independently rather than relying only on the implementer's report.
- Findings include file and line references where applicable and distinguish critical blockers from scored quality gaps.
- Save the report as `docs/reviews/YYYY-MM-DD-<scope>-final-review.md`.

### Final implementation rubric

| Category                                    |  Points | Passing evidence                                                                                                           |
| ------------------------------------------- | ------: | -------------------------------------------------------------------------------------------------------------------------- |
| Requirement and approved-plan conformance   |      25 | The delivered behavior satisfies traced acceptance criteria with no unauthorized scope expansion.                          |
| Correctness and code quality                |      20 | Interfaces and failure behavior are correct; code is maintainable; edge cases and regressions are addressed.               |
| Security, privacy, and tenant isolation     |      20 | Threat controls work in the actual code; authorization fails closed; secrets and student data remain protected.            |
| Test and verification evidence              |      20 | Relevant tests pass; negative and integration coverage is meaningful; typecheck, lint, build, and security checks succeed. |
| Operability, recoverability, and provenance |      15 | Logs/diagnostics, rollback or containment, documentation, dependencies, and copied-source provenance are adequate.         |
| **Total**                                   | **100** | **Acceptance requires more than 85 points.**                                                                               |

### Final decision

- **PASS:** 86/100 or higher and no critical blocker. The implementation may be accepted as complete.
- **FAIL:** 85/100 or lower, or any critical blocker. Fix the findings and obtain a new independent review.
- Do not merge, deploy, or claim completion while final validation is failing or missing.

## Current milestone marker

The Phase 1 identity/tenancy implementation was squash-merged to `origin/main` at `9bb18cbef95963eef5e20654d53cae4723d5efd0`. Its first independent final review scored 77/100 and reproduced a platform-role downgrade bypass; the preserved Attempt 1 verdict is followed by a separate Attempt 2 review in `docs/reviews/2026-08-29-phase-1-identity-tenancy-final-review.md`.

The remediation plan in `docs/superpowers/plans/2026-08-29-identity-tenancy-review-remediation.md` passed its pre-implementation gate at 98/100. Test-first remediation enforces current platform/organization role compatibility, audits conflicts, proves post-write rollback, correlates request trace IDs across safe responses/audits/allowlisted diagnostics, and covers every protected route against missing/malformed/unknown credentials. A new independent non-implementing agent reran the checks and probes and scored the actual remediated implementation **98/100 PASS with no critical blocker** before merge.

The first bounded Phase 2 unit lives on `codex/phase2-wordquest-boss-rules`. Its plan passed the pre-implementation gate at **99/100**. It selectively ports WordQuest boss switch, class-scoped keys, difficulty/HP rules, per-student maximum aggregation, and server-verified contribution projection into the pure `@lessonquest/gamification` package. The implementer check passed 145 tests, lint, formatting, typecheck, builds, audit, and diff validation. A fresh independent non-implementing agent repeated the checks and scored the actual result **95/100 PASS with no critical blocker** in `docs/reviews/2026-08-29-phase-2-wordquest-boss-rules-final-review.md`. Before runtime integration, reject non-string/non-number HP tuning coercions and canonicalize or reject uppercase UUID campaign keys. The package is not wired to an API, database, Firebase, deployment, or real data; Phase 1 M3-M6 and the remaining Phase 2 class/invite/dashboard/PWA/data work remain incomplete. Nothing on this branch has been pushed, merged, or deployed.

The Phase 1 M3–M4 implementation candidate is being built on `codex/phase1-m3-m4-complete` from `origin/main` commit `415d8487fb15627351e260c816fc70041801fb0e`. Its original design and file-level TDD plan passed the pre-implementation gate at **100/100 with no critical blocker**, and its first implementer check passed 176 tests. Independent final-review Attempt 1 then scored the actual implementation **62/100 FAIL** after reproducing three blockers: validation was not bound to the approved artifact/status graph, an in-progress attempt restarted event sequence zero, and client-scripted correctness made teacher results untrustworthy. It also found deadline/state symmetry, audit, shared response-contract, grade-band, and true E2E gaps. The failed verdict is preserved in `docs/reviews/2026-08-29-phase-1-m3-m4-final-review.md`.

The test-first final-gate remediation plan passed a separate pre-implementation review at **100/100 with no blocker**. The candidate now links validation and approval to the canonical artifact hash, enforces forward-only database transitions and validated-content immutability, accepts option IDs instead of client correctness, derives answer outcomes and completion on the server, resumes from server-owned sequence/answer state, applies assignment and institution lifecycle checks at player/event time, records trace-correlated M3/M4 audits, parses shared response contracts, bounds Korean grade bands, and includes a React → HTTP client → Hono → PGlite reload/resume test.

The remediation implementer verification passed on 2026-08-29: `corepack pnpm check` passed 19/19 files and 196/196 tests plus lint, format, typecheck, all workspace builds, and the Vite production build; `test:integration` passed 2/2 files and 13/13 tests; `test:e2e` passed 3/3 files and 8/8 tests across the real React → HTTP → Hono → PGlite boundary; the production dependency audit reported no known vulnerabilities; the changed-file credential-pattern scan and `git diff --check` were clean.

A different fresh independent reviewer then inspected the complete base-to-`486abb8` diff, reran the full verification and adversarial probes, and recorded **91/100 PASS with no critical blocker** as Attempt 2 in `docs/reviews/2026-08-29-phase-1-m3-m4-final-review.md`. The original 62/100 FAIL remains preserved above it. Non-blocking follow-ups before production-adapter work are database-enforced audit-log immutability and distinct duplicate outcomes, exact browser retry after transient delivery failure, real PostgreSQL multi-connection concurrency normalization, a narrower Node/jsdom engine range, and correct-first coverage in the integrated E2E. M3/M4 is complete on the local feature branch but has not been pushed, merged, or deployed. M5, M6, and production adapters remain excluded.

Phase 1 M5–M6 now has a user-approved design at `docs/superpowers/specs/2026-08-30-phase-1-m5-m6-design.md` and an implementation plan at `docs/superpowers/plans/2026-08-30-phase-1-m5-m6.md` on `codex/phase1-m5-m6-complete`, based on local `main` commit `e5e1e04a0dcc02ff094e811e625c11a3b211bccf`. The slice uses a deterministic no-network/no-secret local Rasa provider and at most one active class boss campaign with server-derived contributions and aggregate-only student visibility. The plan also closes the M3/M4 attempt-lock, authoritative acknowledgement/exact-retry, audit immutability/duplicate-outcome, and correct-first E2E follow-ups required by these boundaries.

The M5–M6 pre-implementation review at `docs/reviews/2026-08-30-phase-1-m5-m6-plan-review.md` scored **98/100 PASS with no critical blocker**. The local implementation candidate now contains strict contracts, ACK-based exact event retry, deterministic answer-safe Rasa hints and usage, one active class campaign, authoritative outbox projection, aggregate-only student UI, teacher evidence, and a real HTTP/Hono/PGlite M5/M6 boundary test. Nothing was pushed, merged, deployed, connected to Firebase/external AI, or run against real student data. Final acceptance still requires the fresh full implementer bundle and independent review at 86/100 or above with no blocker.

The fresh implementer verification on 2026-08-30 passed: `corepack pnpm check` completed lint, formatting, typecheck, 24/24 test files with 245/245 tests, every workspace TypeScript build, and the Vite production build; `test:integration` passed 5/5 files and 16/16 tests; `test:e2e` passed 4/4 files and 10/10 tests; the production dependency audit reported no known vulnerabilities; and `git diff --check` was clean. Independent final validation remains mandatory before claiming Phase 1 complete.

Independent M5/M6 final review Attempt 1 then scored **52/100 FAIL** and reproduced critical authorization and answer-safety defects plus advisory timeout, mutable lifecycle evidence, duplicate projection claiming, missing audits, incomplete teacher lifecycle UI, and a missing real React-to-database M5/M6 E2E. The failed verdict is preserved in `docs/reviews/2026-08-30-phase-1-m5-m6-final-review.md`.

The final-gate remediation plan at `docs/superpowers/plans/2026-08-30-phase-1-m5-m6-final-gate-remediation.md` passed a new pre-implementation review at **100/100 with no blocker**. Test-first remediation now authorizes before hint replay, revalidates current authority before finalization, enforces provider timeouts, rejects direct Korean/English choice instructions, installs database state-transition guards, conditionally claims projection jobs, emits the missing audits, completes the teacher campaign lifecycle UI, and adds the planned React → HTTP → Hono → repositories/provider/projector → PGlite fixture. A different independent reviewer must still score the remediated result above 85 with no blocker before completion.

Independent final review Attempt 2 scored **68/100 FAIL**. It confirmed the original three answer phrases were blocked and all standard checks passed, but found additional letter-choice imperative variants, direct campaign deletion and assignment Rasa policy mutation, retryable hint ID loss, incomplete negative E2E evidence, and projection audits with unrelated trace IDs. The verdict is preserved in `docs/reviews/2026-08-30-phase-1-m5-m6-final-review-attempt-2.md`.

Remediation Plan 2 passed at **100/100 with no blocker**. The candidate now blocks Korean and English letter-choice imperatives, protects campaigns from deletion and assignment Rasa policies from mutation, preserves exact hint IDs across retryable responses, replays terminal retryable Rasa failures consistently, and carries ingestion trace IDs through projection jobs into projection audits. A third fresh independent final review remains mandatory.

Independent final review Attempt 3 scored **72/100 FAIL**. It verified the earlier authorization, timeout, retry, campaign/policy mutation, concurrency, success-trace, and teacher lifecycle fixes, but found declarative answer variants, deletable Rasa request/job evidence, reversed failure-audit ID/trace arguments, and remaining E2E/audit gaps. Remediation Plan 3 passed at **100/100 with no blocker**. The candidate now blocks those declarative Korean/English answer forms, rejects request/job deletion, corrects failed projection trace lineage, and expands the real React boundary with reload, terminal-503 exact retry, and campaign end/replacement. A fourth fresh independent final review remains mandatory.

Independent final review Attempt 4 scored **81/100 FAIL** after all prior literal phrases passed, because punctuation inserted inside the same direct-answer statements evaded adjacency-based patterns. Remediation Plan 4 passed at **100/100 with no blocker**. The output boundary now preserves raw-normalized URL/HTML checks while also applying semantic checks to a Unicode NFKC form stripped of whitespace, punctuation, and symbols. All eight reproduced punctuation variants are regression fixtures. A fifth fresh independent final review remains mandatory.

Independent final review Attempt 5 scored **82/100 FAIL** because Unicode format/combining-mark characters and a letter-shaped symbol could still split or erase answer markers. Remediation Plan 5 passed at **100/100 with no blocker**. Semantic normalization now retains only Unicode letters and numbers, and marker-level rules reject direct choice/correct instructions even when a choice glyph disappears. All six reproduced Unicode variants are fixed regression cases. A sixth fresh independent final review remains mandatory.

Independent final review Attempt 6 scored **82/100 FAIL** because Greek/Cyrillic homoglyphs and invisible characters inside URL/HTML structures bypassed checks. Remediation Plan 6 passed at **100/100 with no blocker**. Phase 1 Rasa output now has an explicit printable-ASCII-plus-Hangul alphabet, rejecting all other scripts, format controls, marks, emoji, and symbols; a separate structural canonical form removes invisible characters before URL/HTML detection. The reproduced variants are regression fixtures. A seventh fresh independent final review remains mandatory.

Independent final review Attempt 7 scored **81/100 FAIL** because the remaining open-ended blacklist still accepted novel ASCII direct-answer and URL/markup variants. Remediation Plan 7 passed at **100/100 with no blocker**. The Phase 1 boundary now uses constrained server-owned rendering: the deterministic provider and repository share a pure template renderer, while the repository independently computes and exact-matches the only permitted learner-visible content before persistence. Unknown answer, URL, markup, and code strings cannot enter the accepted output set. An eighth fresh independent final review remains mandatory.

Independent final review Attempt 8 scored **79/100 FAIL** because the constrained renderer still interpolated artifact concept/simulation strings, allowing unsafe server-owned input to influence the exact-matched output. Remediation Plan 8 passed at **100/100 with no blocker**. The renderer is now level-only with three fixed conceptual scaffolds and no artifact/provider text interpolation; hostile concept and simulation inputs are regression-tested across all levels. A ninth fresh independent final review remains mandatory.

Independent final review Attempt 9 scored the complete M5/M6 implementation **88/100 PASS with no critical blocker**. It verified zero influence across 192 hostile concept/simulation combinations, all three fixed local levels, 24/24 arbitrary provider substitutions rejected, 281 tests, 16 integration tests, 14 E2E tests, builds, audit, diff, and credential scan. Remaining non-blocking follow-ups are durable M5/M6 denied/conflict audits and several integrated negative/failure cases.

A separate Vercel static-demo preview is being prepared behind `VITE_DEMO_MODE=true`. It uses only reset-on-refresh synthetic UI state, exposes student Rasa/retry/boss and teacher policy/process/boss views, performs no API/Firebase/external-AI access, and is explicitly not an operating backend. Its plan passed at **98/100**; final independent review is required before it is called ready.

The synthetic student-home visual redesign on `codex/student-home-redesign` uses the user-approved 70% Cosmic Quest / 30% Neo Academy direction. Its bounded plan passed at **97/100 with no critical blocker**. The implementer observed the planned RED state, restored 3/3 focused tests and 14/14 selected web regressions, and completed automated browser inspection at 1440×900, 768×1024, and 375×812 with no horizontal overflow, console errors, focus-ring loss, or reduced-motion violation. Fresh full verification then passed 26/26 files and 284/284 tests with lint, formatting, typecheck, all workspace builds, 16/16 integration tests, 14/14 E2E tests, no known production dependency vulnerabilities, a successful demo build, and a clean diff check. No API, DB, auth, provider, deployment setting, dependency, remote asset, Firebase connection, or real student data changed. Independent final validation is still required before completion can be claimed.

The first independent student-home review scored **90/100 PASS with no critical blocker** but measured the demo role-switch buttons below the approved 44 px minimum and reported three polish/containment findings plus a range-level Markdown whitespace evidence mismatch. The implementation now measures every button and demo-navigation link at 44 px or taller, scopes every new selector below `.demo-shell`, points the initial mission link at an existing target, removes the tablet rail's excess row height, and passes `git diff --check db784b3`. Fresh post-fix implementer verification again passed 284 tests, 16 integration tests, 14 E2E tests, all workspace checks/builds, the production audit, and the three-viewport browser matrix. A second independent final validation of the new HEAD remains mandatory.

The second fresh independent student-home review inspected and retested the complete `db784b3..dc94be0` range and scored the remediated implementation **97/100 PASS with no critical blocker**. It confirmed all five Attempt 1 findings resolved, every target at least 44 px across all interaction states and widths, valid initial fragment targets, compact tablet support placement, fully scoped demo selectors, clean range diff, reset/teacher/privacy behavior, and zero external browser requests. The sole minor follow-up is to make the currently ephemeral browser geometry/network probes durable in the checked-in test suite. The branch is locally ready to merge; it has not been pushed, merged, or deployed.

The user then approved a unified dark, game-forward theme for both synthetic demo roles. The bounded plan passed at **98/100 with no critical blocker**. The implementer observed the shared-theme contract RED, restored 4/4 focused demo tests and 15/15 selected web regressions, and verified 23 computed surfaces per viewport across initial, wrong/hint, completed, and teacher states stayed within the dark luminance threshold at 1440×900, 768×1024, and 375×812. Targets remained at least 44 px, focus and reduced motion passed, and browser traffic contained zero external requests. Fresh full verification passed 26/26 files and 285/285 tests, lint, formatting, typecheck, all workspace builds, 16/16 integration tests, 14/14 E2E tests, the production audit, demo build, and full-range diff check. A new independent final validation of the dark-theme HEAD remains mandatory.

The first independent dark-theme review scored **94/100 PASS with no critical blocker**, while identifying two small uppercase labels whose blue-on-dark contrast measured only 3.05:1 and 3.62:1. A browser regression probe reproduced both failures before the fix. Those labels now use the higher-contrast ion-cyan token and measure at least **8.24:1** at all three target viewports while retaining 23 dark surfaces, 44 px controls, visible focus, reduced-motion behavior, no overflow, and zero external requests. Post-remediation verification again passed 26/26 files and 285/285 tests, lint, formatting, typecheck, all workspace builds, 16/16 integration tests, 14/14 E2E tests, the production audit, demo build, and full-range diff check. Because production CSS changed after Attempt 1, a fresh independent final review of the remediated HEAD remains mandatory.

A different fresh independent dark-theme reviewer inspected and retested the complete `db784b3..d16ebef` range and contrast-remediation slice, scoring the final implementation **97/100 PASS with no critical blocker**. It measured the repaired header label at 9.7729:1 and section labels at 8.2360:1 across 1440x900, 768x1024, and 375x812, and independently confirmed five UI states, 23 dark surfaces, 44 px minimum targets, keyboard focus, reduced motion, no overflow or browser errors, and zero external requests. All 285 tests, 16 integration tests, 14 E2E tests, builds, audit, diff, and containment checks passed. The only minor follow-up is making the currently ephemeral browser contrast/geometry/network probes durable in the checked-in test suite. The local branch now satisfies both implementation gates; it has not been pushed, merged, or deployed.

## Current work — 2026-08-31 durable M5/M6 decision audits

The earlier milestone paragraphs are historical snapshots. PR #4 merged M1–M6 and the reviewed dark demo into `main` at `9bcb2fb777ddae0d85248fe5df822976d17eec8c`. GitHub's Vercel status and Production deployment record `6173146096` both reported success for that commit on 2026-08-31; this is deployment-status evidence, not an operating backend or a new browser check.

The user's next-work request continues the first remaining M5/M6 Attempt 9 finding before production-adapter work. Branch `codex/m5-m6-audit` adds durable trace-correlated denied/conflict audits, correct provider-time revocation classification and duplicate campaign-end evidence. The plan passed at 96/100, revised to 97/100 after discovering and correcting the original revocation test's invalid `REMOVED` status (the schema allows `DISABLED`). Existing authorization, schema, response contracts, provider, UI, dependencies and Vercel configuration are unchanged. Source is original LessonQuest code, with no reference-repository copying.

Baseline verification passed 285 tests. RED reproduced missing audit effects; the fixture correction then isolated wrong revocation outcome and missing end-replay audit. Focused GREEN passed 17 API tests plus the Rasa and boss repository regressions. Full implementer evidence and final independent score are recorded in `docs/reviews/2026-08-31-m5-m6-durable-audits-verification.md` and the accompanying final review. This marker does not itself certify final validation or delivery.

Remaining follow-ups: broader React-to-database ambiguous-delivery/failure matrix, a genuine correct-first M5/M6 React scenario, durable browser geometry/contrast/network tests, then separately planned production adapters or Phase 2 slices. The current Vercel target remains a static synthetic demo; shipping this repository change does not deploy its local API to production.

Independent audit review Attempt 1 scored candidate `c15d25e` **82/100 FAIL** after independently reproducing an inherited disabled-organization boss bypass on all four endpoints. Its report is preserved in `docs/reviews/2026-08-31-m5-m6-durable-audits-final-review.md`; 301 ordinary tests and 12 additional probes passed but did not override the security blocker. A separate remediation plan passed **98/100** and narrowly expands the prior scope to require active organization state in both boss authorization queries. Five new API cases first failed with successful responses for disabled organizations (including exact end replay), before the two active-organization joins were added. The complete remediated result still requires fresh verification and a different independent final reviewer. An inherited nonblocking Rasa finalization diagnostic limitation (generic DB failures stored as authorization-revoked/CONFLICT) remains tracked separately; this slice does not claim to fix it.

A different independent agent reviewed the complete `9bcb2fb..7a461ec` candidate and scored Attempt 2 **96/100 PASS with no critical blocker** in `docs/reviews/2026-08-31-m5-m6-durable-audits-final-review-attempt-2.md`. Its fresh full check passed lint, formatting, types, all builds and **306 tests**; the focused suites passed **24 tests** and separate adversarial checks passed **18 probes**. It confirmed disabled organizations cannot create, end, replay, or read boss campaigns, while reactivation and owner/administrator behavior remain correct. The implementer's final integration and scripted E2E runs passed **37** and **35** tests respectively; those counts include API cases, not only browser tests. The production dependency audit was clean. The runtime candidate now satisfies both mandatory gates; the following documentation-only commit records that result. CI-guarded main integration and the existing static-demo Git deployment must be verified separately before reporting delivery complete.

## Phase 1 closeout and real-UI preview — 2026-08-31

The prior audit slice was delivered in PR #5 at main `fe206c1`. The user requested Phase 1 completion, then explicitly requested a development preview of the actual service on the existing Vercel site. `codex/phase1-closeout` implements the approved97-point closeout plan and96-point preview expansion. The new19 React boundary cases cover full authoring, correct-first7 versus retry8, pre/post-commit transport loss, overlapping actions and negative Rasa/authority outcomes. Rasa finalization faults are distinct from actual authorization loss. The old demo remains a separately tested build; the Vercel candidate instead runs the real teacher/student components, Hono/repositories and synthetic in-memory PGlite in one browser tab. This cannot secure real data and deliberately uses no real login, external AI, shared DB or persistence. Plan/final evidence lives in `docs/reviews/2026-08-31-phase1-closeout-*`; `docs/PHASE1_COMPLETION.md` states the operational limits. This paragraph records an implementation candidate, not final acceptance or deployment; independent final validation and delivery confirmation are pending.

The preceding19-case count describes the initial closeout candidate. Independent closeout reviews Attempt1 (**82FAIL**) and Attempt2 (**85FAIL**) are preserved. The former exposed nonterminal409/404/500 replay clearing a still-running hint; its99-point remediation preserves uncertainty until an established terminal outcome. The latter exposed pending hints across student remount and actual preview role switching; its97-point remediation locks the authenticated attempt and refuses resume while scoped QUEUED/RUNNING requests remain, then restores committed hints/sequence after terminalization. The current three new React boundary files contain **26 cases: flow2, recovery17, negative7**. Focused61 and full339 tests pass, with fresh independent Attempt3 and Git/Vercel delivery still pending. No operating-service readiness, durable worker-crash recovery or multiple-device simultaneous learning is claimed.

Independent closeout Attempt3 then finalized **96/100 PASS with no blocker**, after its own full339 tests,12 adversarial probes and22 Chromium cases, dependency audit, actual preview-header/asset verification and unchanged42-file manifest. Both earlier failed reviews are retained. Phase1's synthetic development flow is now accepted; production authentication/shared persistence/external AI, real students and operating crash recovery remain excluded. Authorized delivery continues through exact-head CI, expected-head merge and the existing Vercel project. The merge PR body records the later main SHA/CI/deployment/live-interaction evidence, so historical pending-delivery paragraphs do not substitute for release verification.

## Cosmic actual-service theme — 2026-08-31

The user requested restoring the prior Higgsfield space-game concept to the real service, then explicitly directed use of that astronaut as the image reference. The96-point plan on `codex/cosmic-service` restores the approved Cosmic70/Neo30 dark direction to real App/Studio/Play through a scoped shell and a locally bundled1.586MB generated illustration. The original concept1 astronaut was inspected and directly supplied to built-in imagegen; exact provenance/prompt is in `docs/COSMIC_ASSET_PROVENANCE.md`. Real handlers, tenant/auth boundaries, runtime identities/reset, sandbox and Vercel flags are unchanged. New tests reject the previous misleading all-stages-active draft indicator and require actual theme/geometry/contrast through eight service states at three widths. Full339 tests,39 integration,63 React boundary cases,12 demo and18 actual-preview Chromium cases passed. The baseline resource-sensitive hook timeout and formatting correction are preserved in the verification report. Independent final acceptance and exact-commit delivery remain pending; this paragraph does not certify them.

Independent Cosmic service final validation scored **96/100 PASS with no critical blocker**, after fresh339 tests,8 focused component cases,18 actual-preview and12 demo Chromium cases, audit and all builds/checks. All14 implementation/test fingerprints matched. The preserved report distinguishes selected contrast/geometry checks from complete WCAG certification and tracks two nonblocking refinements: semantic stage-completion labels and responsive image optimization. Authorized GitHub/Vercel delivery follows this gate; release evidence belongs in the merge PR body.

## Phase 2 first classroom delivery — 2026-08-31

The user requested Phase 2. Following the canon's small-unit approach, the first slice adds class listing/creation/selection, rotating/revocable 24-hour invitations with capacity limits, authenticated self-enrollment, and a current-student assignment dashboard to the real synthetic preview. The prior cosmic slice was delivered in PR #7 at main `2cf2cd3c3b21e101878a9ca9160d985ee33fc568`. The classroom plan passed **95/100** before production edits; independent non-implementing final validation passed **96/100 with no critical blocker**. The actual 24-file implementation/test/script manifest remained unchanged through final review. Records are in `docs/reviews/2026-08-31-phase2-classrooms-*` and the scope/status in `docs/PHASE2_PROGRESS.md`.

Full verification passed **357 tests**, lint/format/types/builds, integration **52**, scripted E2E **67**, legacy demo Chromium **12**, service preview Chromium **21**, and a clean production dependency audit. The independent reviewer separately passed the full check, focused **24** tests, **14** adversarial probes and the final **21** preview cases. Its nonblocking follow-ups are preserving temporary stale-response/authority/rollback probes as durable tests and separately planning idempotent class creation; the current runbook requires a list refresh before retrying an ambiguous creation.

This is acceptance of the first Phase 2 synthetic slice, not completion of all Phase 2 or production readiness. PWA/offline queue and externalAuthId/export preparation remain later units; real Firebase/data transition requires separate approval. No real data, reference-system mutation, dependency change or durable migration occurred. The authorized commit/CI/main/Vercel delivery follows this gate; exact release and live-flow evidence belongs in the merge PR body.

## Phase 2 PWA and offline learning-event candidate — 2026-09-01

The next bounded Phase 2 slice adds a same-origin installable shell and an exact learning-event replay queue. Its design and file-level plan passed the pre-implementation gate at **97/100 with no critical blocker**. The queue is opt-in through an opaque noncredential account storage key plus organization UUID, stores at most 20 strict `ClientLearningEvent` envelopes for 24 hours, and never stores tokens, correctness, content, hints or profiles. The server remains authoritative; terminal authority/lifecycle failures delete the local record rather than moving it between scopes.

Test-first implementation now covers SDK backoff/FIFO/cap/expiry/scope/subscriber behavior, strict pending-session restoration, React pre/post-commit answer and committed-completion recovery through the actual HTTP/Hono/PGlite boundary, terminal deletion, session-ended clearing, IndexedDB data minimization and foreign-scope preservation. The service worker registers only for configured sessions or explicit preview, caches only successful same-origin shell/static responses, and excludes authorization/API/cross-origin/error responses. Public preview reload still creates a fresh synthetic PGlite database; its offline claim is shell availability, not persistent backend readiness.

Implementer verification first passed full tests and a **30-case** service-preview browser matrix without dependency, schema, Firebase, real-data, reference-repository or paid-resource changes. Independent final review Attempt 1 then scored **80/100 FAIL** after reproducing API/authenticated organization responses replacing the offline shell, a redirected cross-origin static response entering the shell cache, and a corrupt current-scope record replaying an embedded foreign-organization event. That failed review remains preserved.

All three findings now have durable RED/GREEN coverage. Navigation requests receive the same authorization/origin/API gate as other cache candidates, final response URLs are rechecked before every write, and both SDK reload plus IndexedDB parsing delete organization-mismatched records before delivery. Fresh implementer verification passes **372 full tests**, integration **52**, scripted E2E **72**, demo browser **12**, and remediated service-preview browser **33**, plus a clean audit and diff check. The final cache probe runs with a test-only relaxed CSP so it verifies the worker response guard independently of the existing production `connect-src 'self'` defense. A fresh independent non-implementing Attempt 2 is still required before merge or deployment; this entry does not certify or release the candidate.

Independent Attempt 2 preserved a second **82/100 FAIL** after its ordinary 372-test and browser evidence passed. A deterministic race left a queued event on the shared device when a retryable send settled after session-end clear/dispose; a separate Chromium probe showed a malformed primary key surviving while replay created a canonical duplicate. The remediation now invalidates every in-flight completion before clear, suppresses stale success/terminal/retryable outcomes, cleans any retry write that crossed clear, and retains the original staged record for ordinary unmount. IndexedDB compares the actual returned key with the canonical scope/event key and deletes only the malformed active-scope record. RED/GREEN coverage includes all three late outcomes through direct clear and the actual host lifecycle, plus unrelated-scope preservation. Fresh implementer verification passes **379 full tests**, integration **52**, scripted E2E **75**, demo browser **12**, service preview **33**, audit and diff. A different independent Attempt 3 must still pass before delivery; this entry does not certify or release the candidate.

Independent Attempt 3 then preserved **80/100 FAIL**. It verified all prior remediations but showed that generation capture began too late: session end could finish while enqueue's initial put or reload/prune remained unresolved, after which the old enqueue persisted its event. Concurrent online enqueues also delivered outside global FIFO and made shared `sending`/`lastAttemptAt` state ambiguous. The queue now captures lifecycle/clear generations before any enqueue await, serializes staging in invocation order, rejects stale prune/put completion, and reserves one FIFO-head delivery with its own attempt timestamp. Actual lifecycle tests cross both put and prune, while the SDK test holds two online deliveries and proves the second is not called before the first settles. Fresh implementer verification passes **382 full tests**, integration **52**, scripted E2E **77**, demo browser **12**, service preview **33**, audit and diff. A different independent Attempt 4 is still required; this entry does not certify or release the candidate.

Independent Attempt 4 matched the exact 26-file implementation/test/script manifest, inspected the actual diff and reran focused **32**, full **382**, integration **52**, E2E **77**, demo browser **12** and service preview browser **33** checks plus audit and adversarial probes. It scored **91/100 PASS with no critical blocker** in `docs/reviews/2026-09-01-phase2-pwa-offline-final-review-attempt-4.md`, so the mandatory implementation gate is satisfied. Two nonblocking follow-ups remain before any persistent-backend rollout: preserve a nonzero retry delay when retry metadata persistence fails, and remove the inert online listener installed by a synchronous reentrant dispose during reload. Authorized exact-head CI, main integration and existing Git-linked Vercel delivery follow; this marker alone does not certify release.

## Phase 2 identity export readiness candidate — 2026-09-01

The third bounded Phase 2 unit adds strict v1 synthetic WordQuest identity-export and explicit LessonQuest mapping contracts plus a pure `@lessonquest/data-transition` reconciliation package. It canonicalizes caller-owned arrays independent of order, pins SHA-256 checksums, reports all duplicate/collision/missing/unused mapping blockers with redacted fingerprints, and counts roles only for ready `STUDENT`/`TEACHER` accounts. Legacy organization keys never authorize. `MASTER` always blocks and never grants a LessonQuest privileged role.

The plan passed **98/100 with no critical blocker**. Test-first implementation produced focused **41**, full **423**, integration **52**, E2E **77**, demo browser **12** and service preview browser **33** passing checks, with a clean production audit and diff. The first emitted-asset probe found transition schema markers entering every web build through the existing contracts root barrel. A new RED/GREEN containment test moved these contracts to the dedicated `@lessonquest/contracts/data-transition` subpath; rebuilt normal/demo/preview JS and source maps contain no transition format, finding-code, package or fixture marker.

This candidate has no Firebase/filesystem/network/database/environment access and no auth, API, DB, web, CLI or schema integration. It uses only synthetic fixtures, adds no external dependency, and does not implement a real exporter, migration or dry run.

Fresh independent final validation inspected the actual diff, reran focused **41**, full **423**, integration **52**, E2E **77**, demo browser **12**, preview browser **33**, audit and adversarial checks, scanned 37 emitted assets and matched all 13 manifest hashes before and after. It scored **94/100 PASS with no critical blocker**. The one moderate nonblocking finding is that C1 controls U+0080–U+009F remain accepted by opaque source identifiers; reject them with durable tests before a real exporter or real identity data consumes this contract. Authorized exact-head CI, main integration and existing Git-linked Vercel verification still follow, so this entry alone does not certify release.
